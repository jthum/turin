# OWNERSHIP

- `runtime` -> Turin kernel and harness execution
- `providers` -> inference provider transport and normalization
- `docs` -> public docs and examples
- `library` -> harness library workflows and blocks
