# CONSTRAINTS

- Prefer forward-looking architecture over legacy compatibility.
- Do not depend on daemon mode or future runtime features.
- Keep the workflow useful even when running against a single provider.
- Persist artifacts that help an operator inspect what happened.
