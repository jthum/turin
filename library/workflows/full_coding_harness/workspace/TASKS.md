# TASKS

- Expand the anchor workflows before broadening the long tail.
- Validate each workflow with deterministic mock-backed tests.
- Keep the workflow harnesses readable enough to be adapted by users.
