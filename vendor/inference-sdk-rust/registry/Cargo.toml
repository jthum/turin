[package]
name = "inference-sdk-registry"
version = "0.7.0"
edition = "2024"

[dependencies]
inference-sdk-core = { path = "../core" }
openai-sdk = { path = "../openai" }
anthropic-sdk = { path = "../anthropic" }
thiserror = "1.0"
futures-util = { version = "0.3", default-features = false, features = ["std"] }
