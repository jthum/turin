# Config Examples

This directory contains copyable configuration examples.

Current layout:

- `examples/config/config.toml.example` — bootstrap runtime config for `.turin/config.toml`
- `examples/config/ui-profiles.toml.example` — shared UI client connection profiles
- `examples/config/channels/` — channel-specific config examples
