[package]
name = "turin-daemon-protocol"
version = "0.23.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
turin-types = { path = "../turin-types" }
