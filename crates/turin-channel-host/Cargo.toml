[package]
name = "turin-channel-host"
version = "0.27.0"
edition = "2024"
description = "Discovery and invocation helpers for independent Turin channel runners"

[dependencies]
anyhow = "1.0"
serde_json = "1.0"
turin-channel-core = { path = "../turin-channel-core" }

[dev-dependencies]
tempfile = "3.10"
