[package]
name = "turin-local-ipc"
version = "0.27.0"
edition = "2024"

[dependencies]
tokio = { version = "1.0", default-features = false, features = ["net", "io-util", "fs"] }

[target.'cfg(windows)'.dependencies]
sha2 = "0.11"

[dev-dependencies]
tempfile = "3.10"
tokio = { version = "1.0", default-features = false, features = ["macros", "rt"] }
