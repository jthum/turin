[package]
name = "turin-ui-core"
version = "0.24.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", default-features = false, features = ["macros", "rt", "sync", "time"] }
toml = "0.8"
turin-control-client = { path = "../turin-control-client" }
turin-daemon-protocol = { path = "../turin-daemon-protocol" }

[dev-dependencies]
tempfile = "3.10"
